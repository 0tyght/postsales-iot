import {useState} from 'react';import {api} from '../services/api';
export default function LoginPage({onLogin}){const[form,setForm]=useState({username:'',password:''}),[error,setError]=useState(''),[loading,setLoading]=useState(false);const submit=async e=>{e.preventDefault();setLoading(true);setError('');try{const data=await api('/auth/login',{method:'POST',body:JSON.stringify(form)});localStorage.setItem('admin_token',data.token);localStorage.setItem('admin_user',JSON.stringify(data.user));onLogin(data.user)}catch(x){setError(x.message)}finally{setLoading(false)}};return <div className="login-page"><div className="login-panel"><div className="login-mark">PS</div><h1>Post-Sales IoT</h1><p>เข้าสู่ระบบจัดการงานหลังการขาย</p>{error&&<div className="alert error">{error}</div>}<form onSubmit={submit}><label>ชื่อผู้ใช้<input autoFocus value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required/></label><label>รหัสผ่าน<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></label><button className="btn primary full" disabled={loading}>{loading?'กำลังเข้าสู่ระบบ...':'เข้าสู่ระบบ'}</button></form></div><div className="login-visual"><div><span>ระบบดูแลหลังการขาย</span><h2>ติดตามลูกค้า อุปกรณ์ และทุกงานบริการในที่เดียว</h2><p>บริหารรอบเซอร์วิส ประกัน เคสปัญหา และงานช่างอย่างเป็นระบบ</p></div></div></div>}
