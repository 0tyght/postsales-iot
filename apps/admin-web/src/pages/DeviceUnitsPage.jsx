import ResourcePage from '../components/ResourcePage';import StatusBadge from '../components/StatusBadge';import useLookups from '../hooks/useLookups';import{fmtDate}from'../services/api';
const warrantyFilter=(row,value)=>{if(value==='none')return!row.warranty_end_date;if(!row.warranty_end_date)return false;const days=(new Date(row.warranty_end_date)-new Date())/86400000;if(value==='expired')return days<0;if(value==='soon')return days>=0&&days<=90;return days>90};
export default function DeviceUnitsPage(){const l=useLookups({models:'/devices/models',sites:'/customer-sites',jobs:'/jobs'});return <ResourcePage title="อุปกรณ์จริง" description="ค้นหา Serial Number ตรวจสถานะ และติดตามประกันของอุปกรณ์แต่ละเครื่อง" createLabel="ลงทะเบียนอุปกรณ์" modalTitle="อุปกรณ์" endpoint="/devices/units" idKey="device_id" lookups={l} initialValues={{device_status:'active'}} filters={[{key:'device_status',label:'สถานะ',options:[{value:'active',label:'ใช้งานอยู่'},{value:'inactive',label:'เลิกใช้งาน'}]},{key:'warranty',label:'ประกัน',predicate:warrantyFilter,options:[{value:'expired',label:'หมดประกันแล้ว'},{value:'soon',label:'หมดภายใน 90 วัน'},{value:'valid',label:'มากกว่า 90 วัน'},{value:'none',label:'ไม่ระบุ'}]}]} columns={[{key:'serial_number',label:'Serial Number'},{key:'model_name',label:'รุ่น'},{key:'customer_name',label:'ลูกค้า'},{key:'site_name',label:'จุดติดตั้ง'},{key:'device_status',label:'สถานะ',render:r=><StatusBadge value={r.device_status}/>},{key:'warranty_end_date',label:'หมดประกัน',render:r=>fmtDate(r.warranty_end_date)}]} fields={[{name:'model_id',label:'รุ่นอุปกรณ์',type:'select',required:true,options:l=>l.models?.map(x=>({value:x.model_id,label:`${x.brand||''} ${x.model_name}`}))||[]},{name:'site_id',label:'ติดตั้งที่',type:'select',required:true,options:l=>l.sites?.filter(x=>x.site_status==='active').map(x=>({value:x.site_id,label:`${x.customer_name} · ${x.site_name}`}))||[]},{name:'installation_job_id',label:'มาจากงานติดตั้ง',type:'select',options:l=>l.jobs?.filter(x=>x.job_type==='installation').map(x=>({value:x.job_id,label:`งาน #${x.job_id} · ${x.site_name}`}))||[],help:'เว้นว่างได้หากเป็นข้อมูลอุปกรณ์เดิม'},{name:'serial_number',label:'Serial Number',required:true,help:'หมายเลขต้องไม่ซ้ำกับเครื่องอื่น'},{name:'device_status',label:'สถานะอุปกรณ์',type:'select',required:true,options:[{value:'active',label:'ใช้งานอยู่'},{value:'inactive',label:'เลิกใช้งาน / ถูกเปลี่ยนแล้ว'}]},{name:'warranty_end_date',label:'วันหมดประกัน',type:'date'}]}/>}
