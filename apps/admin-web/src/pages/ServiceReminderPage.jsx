import{useEffect,useState}from'react';import DataTable from '../components/DataTable';import{api,fmtDate}from'../services/api';
export default function ServiceReminderPage(){const[rows,setRows]=useState([]),[error,setError]=useState(''),[loading,setLoading]=useState(true);const load=()=>api('/customer-sites/service-reminders').then(setRows).catch(e=>setError(e.message)).finally(()=>setLoading(false));useEffect(()=>{load()},[]);const mark=async row=>{if(!confirm(`ยืนยันว่าติดต่อลูกค้า ${row.customer_name} แล้ว?`))return;try{await api(`/customer-sites/${row.site_id}/mark-contacted`,{method:'POST'});await load()}catch(e){setError(e.message)}};return <section><div className="page-head"><div><h1>รอบเซอร์วิส</h1><p>รายการติดต่อลูกค้า เมื่อบันทึกแล้วระบบจะคำนวณรอบถัดไปอัตโนมัติ</p></div></div>{error&&<div className="alert error">{error}</div>}{loading?<div className="loading">กำลังโหลดข้อมูล...</div>:<DataTable idKey="site_id" rows={rows} columns={[{key:'customer_name',label:'ลูกค้า'},{key:'site_name',label:'จุดติดตั้ง'},{key:'phone',label:'โทรศัพท์'},{key:'service_interval_days',label:'ติดต่อทุก',render:r=>`${r.service_interval_days} วัน`},{key:'last_service_contact_date',label:'ติดต่อล่าสุด',render:r=>fmtDate(r.last_service_contact_date)},{key:'next_service_contact_date',label:'กำหนดครั้งถัดไป',render:r=>fmtDate(r.next_service_contact_date)},{key:'days_until_contact',label:'สถานะ',render:r=>r.days_until_contact<0?`เกินกำหนด ${Math.abs(r.days_until_contact)} วัน`:`อีก ${r.days_until_contact} วัน`}]} rowActions={[{label:'บันทึกว่าติดต่อแล้ว',onClick:mark}]}/>}</section>}
