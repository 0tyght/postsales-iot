import ResourcePage from '../components/ResourcePage';import StatusBadge from '../components/StatusBadge';import{fmtDate}from'../services/api';
export default function WarrantyAlertPage(){return <ResourcePage title="ประกันอุปกรณ์" description="ติดตามวันหมดประกันของอุปกรณ์แต่ละชิ้น" endpoint="/devices/warranty-alerts" idKey="device_id" readOnly columns={[{key:'serial_number',label:'Serial Number'},{key:'model_name',label:'รุ่น'},{key:'site_name',label:'จุดติดตั้ง'},{key:'device_status',label:'สถานะ',render:r=><StatusBadge value={r.device_status}/>},{key:'warranty_end_date',label:'หมดประกัน',render:r=>fmtDate(r.warranty_end_date)},{key:'days_remaining',label:'คงเหลือ',render:r=>r.days_remaining<0?`หมดแล้ว ${Math.abs(r.days_remaining)} วัน`:`${r.days_remaining} วัน`}]}/>}
